<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{{ room.name }}</title>

        <!-- Bootstrap core CSS -->
        {% stylesheets  'css/bootstrap.min.css'
                        'css/homepage.css'
                        'css/rooms.css' filter='cssrewrite' %}
            <link rel="stylesheet" href="{{ asset_url }}" />
        {% endstylesheets %}

        <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
        <!--[if lt IE 9]>
            <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
            <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
        <![endif]-->
    </head>

    <body>
        <nav class="navbar navbar-inverse navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <a class="navbar-brand" href="#">Plug-like</a>
                </div>
                <div id="navbar" class="collapse navbar-collapse">
                    <ul class="nav navbar-nav">
                        <li><a href="{{ path('eliastre100_dashboard_homepage') }}">Dashboard</a></li>
                    </ul>
                </div>
            </div>
        </nav>

        <div class="container">

            <div class="starter-template">
                <h1>There is your room page !</h1>
                <p class="lead">
                    <p>This page will be the main page of your website so make it fun !<br>
                    <hr />
                    <p>You can customize this page by editing the src/Eliastre100/RoomsBundle/resouces/views/Rooms/base.html.twig<br />Note that for all page you can overload all pages <a target="_blank" href="http://symfony.com/doc/current/cookbook/bundles/override.html" >according to the documentation</a></p>
                </p>
            </div>
        </div>
        <nav class="navbar navbar-inverse navbar-fixed-bottom">
            <div class="navbar-header" id="room_title">
                <a class="navbar-brand" href="#">{{ room.name }}</a>
            </div>
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    <li><a onclick="display_manager();"><span class="glyphicon glyphicon-chevron-up"></span></a> </li>
                </ul>
            </div>
        </nav>

        <!-- PANELS -->
        <div id="music_manager">
            <aside class="playlist_container">
                {{ form_start(form, {'attr': {'id': 'form_create_playlist'}}) }}
                    {{ form_widget(form) }}
                {{ form_end(form) }}
                <ul id="playlists"></ul>
            </aside>
            <content class="music_container">
                <form id="video" action="#">
                    <p><input type="text" id="search" placeholder="Type something..." autocomplete="off" class="form-control" /></p>
                    <p><input type="submit" value="Search" class="form-control btn btn-primary w100"></p>
                </form>
                <div id="search"><ul id="results"></div>
            </content>
        </div>

        {% stylesheets  'js/jquery-1.9.0.js'
            'js/room.js'
            'js/playlists.js'
            'js/video.js' %}
            <script src="{{ asset_url }}"></script>
        {% endstylesheets %}
        <script src="https://apis.google.com/js/client.js?onload=init"></script>
    </body>
</html>
